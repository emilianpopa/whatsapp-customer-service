# Anthropic API key for Claude
ANTHROPIC_API_KEY=sk-ant-...

# Flask secret key
FLASK_SECRET_KEY=change-me-to-a-random-string

# WhatsApp session directory (persistent storage)
WHATSAPP_SESSION_DIR=/root/.whatsapp_session

# Server port
PORT=8080

# Optional: Slack webhook for notifications
SLACK_WEBHOOK_URL=
